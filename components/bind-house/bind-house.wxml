<view class="bind-house">
  <view class="bind-house-title">{{title}}</view>
  <view class="bind-house-message">{{message}}</view>
  <view class="bind-house-btn" bindtap="handleBindHouse">{{btn}}</view>
</view>